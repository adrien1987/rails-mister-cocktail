
<%= render 'shared/wrapper' %>

  <div class="container">
    <div class="row">
        <% @cocktails.each do |cocktail| %>
          <%= render 'shared/cards', cocktail: cocktail %>
        <% end %>
      </ul>
    </div>
  </div>
